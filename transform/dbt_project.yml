name: footballiq
version: "1.0.0"
profile: footballiq

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
seed-paths: ["seeds"]

seeds:
  footballiq:
    +schema: silver  # governance/reference data lives with conformed layer

models:
  footballiq:
    silver:
      +schema: silver
      +materialized: table
    gold:
      +schema: gold
      +materialized: table
