"""Domains — pluggable business domains.

Each subpackage is one business domain built on the kernel.
`football` is the demonstration domain; a manufacturing or finance
deployment adds a sibling package without touching the rest of the platform.
Depends on: kernel only.
"""
