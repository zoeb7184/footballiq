"""Kernel — domain-agnostic building blocks.

Generic abstractions reused by every domain: base Entity, ValueObject,
Repository and UnitOfWork ports, domain events, Result types.
Depends on: nothing (standard library only). Nothing here may import
from any other footballiq package.
"""
