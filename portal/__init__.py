"""FootballIQ customer portal — a Streamlit client over the API only.

Proves API-contract sufficiency (xai-design §5): every page reads through the
public HTTP API with an API key; nothing here touches the warehouse directly.
"""
