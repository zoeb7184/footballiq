# Server-side only — never exposed to the browser (BFF proxy injects the key).
FIQ_API_URL=http://localhost:8000
FIQ_API_KEY=dev-local-key
# Public URL used for metadata/OG.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
